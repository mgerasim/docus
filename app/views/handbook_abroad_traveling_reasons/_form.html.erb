<%= form_for(@handbook_abroad_traveling_reason) do |f| %>
  <% if @handbook_abroad_traveling_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@handbook_abroad_traveling_reason.errors.count, "error") %> prohibited this handbook_abroad_traveling_reason from being saved:</h2>

      <ul>
      <% @handbook_abroad_traveling_reason.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
