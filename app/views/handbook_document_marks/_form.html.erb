<%= form_for(@handbook_document_mark) do |f| %>
  <% if @handbook_document_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@handbook_document_mark.errors.count, "error") %> prohibited this handbook_document_mark from being saved:</h2>

      <ul>
      <% @handbook_document_mark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
