<h1>Listing handbook_place_employees</h1>

<table>
  <tr>
    <th>Fullname</th>
    <th>Shortname</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @handbook_place_employees.each do |handbook_place_employee| %>
  <tr>
    <td><%= handbook_place_employee.fullname %></td>
    <td><%= handbook_place_employee.shortname %></td>
    <td><%= link_to 'Show', handbook_place_employee %></td>
    <td><%= link_to 'Edit', edit_handbook_place_employee_path(handbook_place_employee) %></td>
    <td><%= link_to 'Destroy', handbook_place_employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Handbook place employee', new_handbook_place_employee_path %>
