<h1>Справочник "Подразделение"</h1>

<table>
  <tr>
    <th><%= HandbookDivision.human_attribute_name(:fullname) %></th>
    <th><%= HandbookDivision.human_attribute_name(:shortname) %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @handbook_divisions.each do |handbook_division| %>
  <tr>
    <td><%= handbook_division.fullname %></td>
    <td><%= handbook_division.shortname %></td>
    <td><%= link_to 'Показать', handbook_division %></td>
    <td><%= link_to 'Редактировать', edit_handbook_division_path(handbook_division) %></td>
    <td><%= link_to 'Удалить', handbook_division, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить', new_handbook_division_path %>
